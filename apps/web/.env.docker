# =============================================================================
# Docker Compose local development environment
# =============================================================================
# Copy this file to .env.local to use local Docker services:
#   cp apps/web/.env.docker apps/web/.env.local
#
# Then start the infrastructure:
#   pnpm docker:up
#   pnpm db:push
#   pnpm dev
# =============================================================================

# Deployment environment: "dev" = local Docker, unset = Vercel + NeonDB
DEPLOYMENT_ENV=dev
NEXT_PUBLIC_IS_DEMO=true
CRON_SECRET=UT5FB/gjEJPduMr++Z9s6Mx2xaFIeN5cEaB6oegkBdI=
# PostgreSQL (Docker)
DATABASE_URL=postgresql://lgtm:lgtm@localhost:5432/lgtm
NEXT_PUBLIC_REGISTRATION_OPEN=false

# Auth
BETTER_AUTH_SECRET=your-secret-key-at-least-32-characters-long
BETTER_AUTH_URL=http://localhost:3000

# Storage: SeaweedFS (Docker)
STORAGE_PROVIDER=s3
S3_ENDPOINT=http://localhost:8334
S3_REGION=us-east-1
S3_BUCKET=lgtm
S3_ACCESS_KEY_ID=lgtm_dev_access_key
S3_SECRET_ACCESS_KEY=lgtm_dev_secret_key
S3_PUBLIC_URL_BASE=http://localhost:3000/storage